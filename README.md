terraform/
├── ec2-provisioning/        # Local state (basic)
├── ec2-bootstrap/           # EC2 + Docker + Kubernetes
└── ec2-remote-state/        # EC2 + S3 backend + DynamoDB locking

Currently working on ec2-bootstrap